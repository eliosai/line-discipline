# Open work

Only unfinished work belongs here. Git history carries completed plans and measurements.

## 1.x

- Bound the echo `VSTOP` holds the way the kernel's 3808 byte discard watermark does
- Break, parity and overrun flags for a serial line, which a pty never raises
